[pytest]
testpaths      = tests
addopts        = -v --tb=short
timeout        = 180
markers        =
    unit:      Pure Python tests, no server or DB required
    auth:      Authentication endpoint tests
    isolation: Multi-tenant data isolation tests
    pipeline:  End-to-end pipeline tests (hits Vertex AI, costs API calls)
