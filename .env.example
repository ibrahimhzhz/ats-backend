# Google Cloud Vertex AI Configuration

# Your Google Cloud Project ID (Required)
GCP_PROJECT_ID=your-project-id-here

# Google Cloud Region (Optional - defaults to us-central1)
GCP_LOCATION=us-central1

# Path to your Google Cloud service account JSON key file (Optional)
# If not specified, will use Application Default Credentials (ADC)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your-service-account-key.json

# Auth / JWT Configuration
# Required in all non-local environments (>= 32 chars recommended)
SECRET_KEY=replace-with-a-strong-random-secret-at-least-32-characters
JWT_ISSUER=ats-backend
JWT_AUDIENCE=ats-client

# Local development escape hatch only.
# Set to true only if you intentionally want to run locally without a strong SECRET_KEY.
ALLOW_INSECURE_DEV_SECRET=true

# Database (optional, defaults to SQLite)
# DATABASE_URL=postgresql://user:pass@localhost/ats_db

# In-memory job tracker cleanup (until Redis/Celery migration)
JOB_TRACKER_CLEANUP_INTERVAL_SECONDS=1800
JOB_TRACKER_MAX_AGE_HOURS=24
